- name: variables from command line
  hosts: localhost
  tasks:
  - name: print variables from command line
    ansible.builtin.debug:
      msg: "The value of variable course is : {{COURSE}}" #give -e COURSE=DEVOPS after password in command line at run time